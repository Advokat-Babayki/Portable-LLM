name: hello diagnostic

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello from CI"

# fleet: renamed 2026 to reset push-event router
